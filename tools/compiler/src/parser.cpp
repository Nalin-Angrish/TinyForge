// Parser for FlexNN's flat binary (see docs/LLD.md §5)
// Reuses LayerHeader struct from FlexNN's ModelIO (copied)
#include <cstdint>
namespace tinyforge { namespace compiler {
// TODO: implement parser that reads magic, version, layer headers, and blobs
} }
